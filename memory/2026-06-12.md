# 2026-06-12

## AI Governance refresh

- Updated Ashfall governance adoption against `E:\BackUp\Git_EE\ai-governance-framework`.
- Framework checkout was fetched from `origin/main`; local framework HEAD used for final adoption: `8a4262aeff0ff41a4372a37819d6059fc5629cc2`.
- F-7 apply installed/verified AI Governance hooks, Copilot instructions, `governance/framework.lock.json`, and AGENTS F-7/memory workflow anchors.
- `adopt_governance.py` refreshed `.governance/baseline.yaml`, preserved existing `AGENTS.md`, `PLAN.md`, and `contract.yaml`, and added missing canonical governance docs.
- Verification:
  - `governance_drift_checker.py --format json`: `ok=true`, `severity=ok`.
  - `external_repo_readiness.py --format json`: `ready=true`.
  - `f7_full_update --format json`: `f7_final_status=completed`.

## Build-gating thin slice

- Added `experiments/build_driven_slice/BUILD_DRIVEN_SLICE_PLAN.md` as the repo-contract for a thin slice over the existing runtime.
- Scope decision: reuse `CharacterProfile`, `get_available_options()`, `resolve_event_choice()`, and `RunEngine`; do not create a parallel experiment engine.
- Implemented compact `requirements` mapping for authoring (`STR/PER/INT/CHA/SUR`, `trait`) into runtime `character_filters`.
- Added locked-visible option metadata (`locked`, `locked_text`, `lock_reasons`) and `visible_if_locked=false` hidden behavior.
- Updated CLI event resolution to pass `run.flags` into option display and choice resolution.
- Verification:
  - `python -m pytest tests/test_build_driven_slice_contract.py tests/test_trait_flow.py`: 4 passed.
  - `python -m pytest tests/test_v10_character_system.py::test_special_filtering`: 1 passed.
  - `python -m py_compile src/event_engine.py scripts/play_cli_run.py`: passed.

## Build-gating product/platform contract

- Added `experiments/build_driven_slice/BUILD_GATING_PRODUCT_CONTRACT.md` as the product contract layer above the implementation plan.
- Product direction: build-driven wasteland RPG roguelite.
- First public validation target: Steam Demo.
- Platform sequencing: PC/Web validation first, Steam Demo / itch-style external testing second, iPad/iOS adaptation later.
- Contract boundary: iOS is a later target, not the first validation platform; do not let mobile UI, App Store, TestFlight, or mobile monetization drive v0.1/v0.2.

## P3 event specification gate

- Added `experiments/build_driven_slice/P3_EVENT_SPECS.md`.
- Decision: do not implement the five deterministic gameplay events until the P3 specs satisfy the product-contract checklist.
- P3 spec now defines `roadside_trap`, `locked_clinic`, `infection_checkpoint`, `underground_market`, and `vault_gate` as build-reactivity events rather than generic wasteland events.
- Each event spec records common option, build-gated option, locked-visible temptation, flag set/read behavior, end-summary consequence, and product pillar coverage.

## P3 event payloads

- Added build-driven experiment payloads under `experiments/build_driven_slice/events/` for `roadside_trap`, `locked_clinic`, `infection_checkpoint`, `underground_market`, and `vault_gate`.
- Kept payloads under `experiments/` instead of `schemas/samples/events/` because the global Phase A event schema does not yet include build-gating fields such as `requirements`, `visible_if_locked`, and `locked_text`.
- Added `tests/test_build_driven_p3_payloads.py` to validate event sequence, common/build-gated/locked option shape, flag set/read coverage, ending signatures, option divergence, locked temptations, and a Vault Mechanic technical-ending flag path.
- Verification:
  - `python -m pytest tests/test_build_driven_p3_payloads.py tests/test_build_driven_slice_contract.py tests/test_trait_flow.py`: 7 passed.
  - `python scripts/validate_phase_a.py`: passed.

## P4 build-gating UI contract

- Added `experiments/build_driven_slice/BUILD_GATING_UI_CONTRACT.md`.
- Decision: P4 UI is a build reactivity viewer, not a generic text RPG UI or decorative wasteland dashboard.
- The UI must always answer: current build, build-opened options, locked temptations, and consequences/flags from earlier choices.
- Platform stance remains PC/Web validation for a future Steam Demo; responsive and iPad-safe is acceptable, but iOS-first is explicitly out of scope.
- P4 execution order is fixed as:
  - P4-UI-A: UI contract.
  - P4-UI-B: static hardcoded prototype.
  - P4-UI-C: read P3 payloads from `experiments/build_driven_slice/events/`.
  - P4-UI-D: instrumented player/debug summary.
- Required P4-UI-B static states: Vault Mechanic at `locked_clinic`, Wasteland Grifter at `infection_checkpoint`, and Ex-Raider at `underground_market`.

## P4 static build reactivity prototype

- Added `ui/src/components/BuildDrivenPrototype.jsx` and wired it from `ui/src/App.jsx` through `BaseView`.
- Added backend-offline fallback data in `BaseView` so the P4-UI-B static prototype remains accessible without FastAPI.
- Added responsive CSS for build selection, event scene, available choices, locked temptations, character/resources, run timeline, consequence log, and run summary.
- Verification:
  - `npm run build` in `ui/`: passed.
  - In-app browser desktop check at `http://127.0.0.1:5174/`: viewer rendered with 7 panels and 5 choice cards.
  - In-app browser 390px check: viewer rendered with 7 panels and no horizontal overflow (`scrollWidth=390`, `clientWidth=390`).
  - `npm run lint` in `ui/`: failed on existing lint surfaces including `react-hooks/set-state-in-effect` in `App.jsx` / `BaseView.jsx`, undefined helpers in `BuildAnalysis.jsx`, and an unused `onCancel` prop in `CharacterCreator.jsx`.

## P4 UI refresh

- Changed the default app entry from the old Base/HUD flow to the build-driven prototype view.
- Refreshed `BuildDrivenPrototype.jsx` from a generic dashboard layout into an Ashfall Build Test Bench: build rail, event dossier, evidence strip, available choices, locked temptations, consequence log, and run summary.
- Reworked the build-driven CSS away from the old bright green glass/cyberpunk HUD toward a restrained dossier/test-bench style using dark surfaces, muted paper text, rust/cyan/amber status colors, hard panel edges, and route-trace visual treatment.
- Verification:
  - `npm run build` in `ui/`: passed.
  - Browser desktop at `http://127.0.0.1:5174/`: first screen is `Ashfall Build Test Bench`, old `Forward Operating Base` text absent, 7 panels, no horizontal overflow.
  - Browser 390px: first screen remains the new test bench, old Base absent, 7 panels, no horizontal overflow (`scrollWidth=390`, `clientWidth=390`).

## P4 Chinese UI and scene images

- Translated the build-driven prototype visible UI text into Traditional Chinese.
- Added local scene SVG assets:
  - `ui/src/assets/clinic-scene.svg`
  - `ui/src/assets/checkpoint-scene.svg`
  - `ui/src/assets/market-scene.svg`
- Wired each static prototype state to its scene image so the event dossier has a real visual asset instead of a CSS-only route trace.
- Verification:
  - `npm run build` in `ui/`: passed.
  - Browser desktop: heading is `Ashfall Build 驗證台`, English `Available Choices` / old `Ashfall Build Test Bench` text absent, scene image loaded.
  - Browser state switching: Grifter shows `感染檢查站` image/text; Raider shows `地下市場` image/text.
  - Browser 390px: Chinese UI and image render with no horizontal overflow (`scrollWidth=390`, `clientWidth=390`).

## P4 UI payload adapter

- Updated `ui/src/components/BuildDrivenPrototype.jsx` from hardcoded option state to direct imports from `experiments/build_driven_slice/events/*.json`.
- Added a UI adapter that converts P3 payload options into `ChoiceView` fields: `kind`, `requirementLabel`, `lockedText`, and `consequencePreview`.
- Kept player-facing text in Traditional Chinese through a presentation copy layer while preserving raw payload option ids in the debug line.
- Added `tests/test_build_driven_ui_payload_adapter.py` to prevent the UI from drifting back to hardcoded-only state.
- Verification:
  - `python -m pytest tests/test_build_driven_ui_payload_adapter.py tests/test_build_driven_p3_payloads.py tests/test_build_driven_slice_contract.py tests/test_trait_flow.py`: 10 passed.
  - `npm run build` in `ui/`: passed.
  - Browser desktop: payload-backed mode rendered with P3 option ids and Chinese copy.
  - Browser 390px: payload-backed mode rendered without horizontal overflow (`scrollWidth=390`, `clientWidth=390`).

## P4 instrumented player/debug summary

- Added an instrumented summary path in `ui/src/components/BuildDrivenPrototype.jsx` for comparing the three preset builds against the same P3 event payload sequence.
- Summary fields are intentionally machine-readable: `build_id`, `build_options_taken`, `locked_options_seen`, `flags_triggered`, `flags_consumed`, `ending_id`, and `death_or_win_reason`.
- The active build now shows a right-column `Machine Summary`, and the page includes a three-build `Instrumented Summary` comparison row.
- Added CSS for compact debug lines and summary comparison cards.
- Added a focused test assertion so the UI does not regress away from the instrumented summary contract.
- Verification:
  - `python -m pytest tests/test_build_driven_ui_payload_adapter.py tests/test_build_driven_p3_payloads.py tests/test_build_driven_slice_contract.py tests/test_trait_flow.py`: 11 passed.
  - `npm run build` in `ui/`: passed.
  - `git diff --check`: passed with Windows LF-to-CRLF warnings only.
  - Browser desktop at `http://127.0.0.1:5174/`: `Machine Summary`, three `Instrumented Summary` cards, `ending_id`, and scene image rendered with no horizontal overflow.
  - Browser 390px: summary cards remain present, image loaded, and no horizontal overflow (`scrollWidth=390`, `clientWidth=390`).

- memory_type: session-derived
  record_format_version: 1.0
  writer: governance_tools.memory_record
  what_changed: P4-UI-D added payload-backed instrumented summaries for the three build presets, including active-build Machine Summary and a three-build Instrumented Summary comparison in the React prototype.
  commit: f0db6c2
  commit_hash: f0db6c2
  session_id: cli-20260612-184304
  memory_binding: bound
  test_evidence: python -m pytest tests/test_build_driven_ui_payload_adapter.py tests/test_build_driven_p3_payloads.py tests/test_build_driven_slice_contract.py tests/test_trait_flow.py => 11 passed; npm run build in ui => passed; git diff --check => LF-to-CRLF warnings only; browser desktop and 390px checks confirmed Machine Summary, three summary cards, image loaded, and no horizontal overflow.
  next_step: Decide whether the next slice should package this as a Steam-demo-facing web prototype flow or extend runtime-backed playthrough simulation first.
  plan_reconciliation: updated

## Steam demo-facing web prototype flow

- Converted the P4 build-driven viewer from a mostly static validation bench into a clickable PC/Web demo flow.
- The UI now supports choosing one of the three build presets, starting the run, clicking available choices through all five P3 events, carrying flags/resources forward, showing locked temptations each event, and rendering an ending screen.
- Kept the debug contract visible through the active-run `Machine Summary` and the three-build `Instrumented Summary` comparison row.
- Added a focused UI contract test for the Steam demo flow entry points: `startRun`, `handleChooseOption`, `buildDemoRunSummary`, `Demo Start`, and `Demo Ending`.
- Verification:
  - `python -m pytest tests/test_build_driven_ui_payload_adapter.py tests/test_build_driven_p3_payloads.py tests/test_build_driven_slice_contract.py tests/test_trait_flow.py`: 12 passed.
  - `npm run build` in `ui/`: passed.
  - Browser 390px: clicked Vault Mechanic through five events to `vault_entry_technical_success`, 5 log entries, no horizontal overflow (`scrollWidth=390`, `clientWidth=390`).
  - Browser 1200px: ending remained visible, three summary cards present, image loaded, no horizontal overflow (`scrollWidth=1200`, `clientWidth=1200`).

- memory_type: session-derived
  record_format_version: 1.0
  writer: governance_tools.memory_record
  what_changed: Converted the build-driven React prototype into a Steam-demo-facing web flow: choose build, start run, click through five P3 events, carry flags/resources, show locked temptations, render ending, and keep active-run Machine Summary plus three-build Instrumented Summary.
  commit: f0db6c2
  commit_hash: f0db6c2
  session_id: cli-20260612-190951
  memory_binding: bound
  test_evidence: python -m pytest tests/test_build_driven_ui_payload_adapter.py tests/test_build_driven_p3_payloads.py tests/test_build_driven_slice_contract.py tests/test_trait_flow.py => 12 passed; npm run build in ui => passed; browser 390px clicked Vault Mechanic through five events to vault_entry_technical_success with no horizontal overflow; browser 1200px showed ending, three summary cards, image loaded, no horizontal overflow.
  next_step: Decide whether to add runtime-backed save/export logs or broaden the demo with one more build-reactive branch.
  plan_reconciliation: updated
